# Docs moved

Specifications, roadmaps, and build plans are **not** kept under `RES/` so they stay separate from runtime career data and the Streamlit app.

See **[`../../docs/RES/`](../../docs/RES/)** (start with [`SPEC.md`](../../docs/RES/SPEC.md)).
